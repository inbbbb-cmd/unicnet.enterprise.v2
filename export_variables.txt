# Переменные окружения для экспорта
# Используйте: source export_variables.txt
# Или: export $(grep -v '^#' export_variables.txt | xargs)

# PostgreSQL (используется Keycloak)
export POSTGRES_DB=unicnetdb
export POSTGRES_USER=unicnet
export POSTGRES_PASSWORD=postgres123

# MongoDB - root пользователь (для инициализации)
export MONGO_INITDB_ROOT_USERNAME=unicnet
export MONGO_INITDB_ROOT_PASSWORD=mongo123

# MongoDB - основная база данных
export MONGO_INITDB_DATABASE=unicnet_db

# MongoDB - UnicNet
export MONGO_UNICNET_DB=unicnet_db
export MONGO_UNICNET_USER=unicnet
export MONGO_UNICNET_PASSWORD=unicnet_pass_123

# MongoDB - Logger
export MONGO_LOGGER_DB=logger_db
export MONGO_LOGGER_USER=logger_user
export MONGO_LOGGER_PASSWORD=logger_pass_123

# MongoDB - Vault
export MONGO_VAULT_DB=vault_db
export MONGO_VAULT_USER=vault_user
export MONGO_VAULT_PASSWORD=vault_pass_123

# Keycloak
export KEYCLOAK_ADMIN_USER=unicnet
export KEYCLOAK_ADMIN_PASSWORD=admin123

# Лицензия (используется всеми сервисами). Замените на реальное значение перед запуском
# Если лицензия уже экспортирована в shell заранее, эта строка её не перезатрёт.
# Иначе оставьте пусто и заполните вручную: export UniCommLicenseData="<ваша_лицензия>"
export UniCommLicenseData="${UniCommLicenseData:-}"

# Router (unicnet.router).
# Для инсталляции за NAT указывайте внутренние CIDR локальных сетей (RFC1918), а не внешний WAN-адрес.
# Можно указать несколько подсетей через запятую без пробелов, например:
# 10.10.0.0/16,192.168.100.0/24,172.20.30.0/24
# Как узнать CIDR на сервере: ip addr или ip route.
# Аналогично: сохранит уже заданный RouterCidr, если он был экспортирован ранее.
export RouterCidr="${RouterCidr:-}"
# ============================================
# Как удалить переменные окружения
# ============================================
# Для удаления переменной окружения используйте команду unset:
# 
# unset  RouterCidr
# unset POSTGRES_DB
# unset POSTGRES_USER
# unset POSTGRES_PASSWORD
# unset MONGO_INITDB_ROOT_USERNAME
# unset MONGO_INITDB_ROOT_PASSWORD
# unset MONGO_INITDB_DATABASE
# unset MONGO_UNICNET_DB
# unset MONGO_UNICNET_USER
# unset MONGO_UNICNET_PASSWORD
# unset MONGO_LOGGER_DB
# unset MONGO_LOGGER_USER
# unset MONGO_LOGGER_PASSWORD
# unset MONGO_VAULT_DB
# unset MONGO_VAULT_USER
# unset MONGO_VAULT_PASSWORD
# unset KEYCLOAK_ADMIN_USER
# unset KEYCLOAK_ADMIN_PASSWORD
# unset UniCommLicenseData
#
# Или удалите все переменные одной командой:
# unset RouterCidr \
#       POSTGRES_DB POSTGRES_USER POSTGRES_PASSWORD \
#       MONGO_INITDB_ROOT_USERNAME MONGO_INITDB_ROOT_PASSWORD MONGO_INITDB_DATABASE \
#       MONGO_UNICNET_DB MONGO_UNICNET_USER MONGO_UNICNET_PASSWORD \
#       MONGO_LOGGER_DB MONGO_LOGGER_USER MONGO_LOGGER_PASSWORD \
#       MONGO_VAULT_DB MONGO_VAULT_USER MONGO_VAULT_PASSWORD \
#       KEYCLOAK_ADMIN_USER KEYCLOAK_ADMIN_PASSWORD UniCommLicenseData
#
# Проверить, что переменная удалена:
# echo $POSTGRES_DB
# (должно быть пусто)
